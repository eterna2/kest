version: 2
updates:
  # GitHub Actions — pin and keep action SHAs up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "github-actions"
    groups:
      github-actions-dependencies:
        patterns:
          - "*"
    # Security updates are handled separately and trigger faster via GitHub's
    # automatic security update PRs (not governed by this schedule).

  # Rust / Cargo (kest-core native Rust extension)
  - package-ecosystem: "cargo"
    directory: "/libs/kest-core/python"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "rust"
    groups:
      cargo-dependencies:
        patterns:
          - "*"

  # Python (pip/uv ecosystem)
  - package-ecosystem: "pip"
    directory: "/libs/kest-core/python"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "python"
    groups:
      pip-dependencies:
        patterns:
          - "*"

  # Node / Bun (website)
  - package-ecosystem: "npm"
    directory: "/website"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "frontend"
    groups:
      npm-dependencies:
        patterns:
          - "*"
